# Dashboard Configuration
# Copy this file to .env and fill in your values
# On Railway: set these as environment variables in the Railway dashboard

# Server (Railway sets PORT automatically)
PORT=3000

# Data directory for SQLite database
# On Railway: set to /data and attach a volume at /data
# DATA_DIR=/data

# JWT Secret — generate a strong random string (64+ chars)
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_ME_TO_A_RANDOM_SECRET

# Session duration in hours
SESSION_HOURS=24

# Allowed origins for CORS (comma-separated)
# Include your dashboard domain so the login page works
ALLOWED_ORIGINS=https://gabbybmusic.com,https://www.gabbybmusic.com,https://dash.gabbybmusic.com

# Rate limiting
LOGIN_MAX_ATTEMPTS=5
LOGIN_WINDOW_MINUTES=15
